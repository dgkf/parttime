% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class_partial_time_compat_stats.R
\name{xtfrm.partial_time}
\alias{xtfrm.partial_time}
\title{Rank a partial_time for base sorting and ordering}
\usage{
\method{xtfrm}{partial_time}(x)
}
\arguments{
\item{x}{a partial_time object}
}
\value{
An integer vector of ranks.
}
\description{
\code{base::sort()} and \code{base::order()} reach a classed vector through
\code{\link[base:xtfrm]{base::xtfrm()}}.  Without a method they do not fall back to anything
meaningful for a record type: \code{order()} returns the identity permutation and
\code{sort()} returns an empty vector, both without a warning.  This makes them
agree with \code{\link[vctrs:vec_order]{vctrs::vec_order()}}, which ranks by component from the year down.
}
\details{
A wholly missing value ranks \code{NA}, so \code{sort()} drops it as it does for any
other vector.
}
\examples{
x <- as.parttime(c("2015-04-13", "2015", "2015-04", "2014-12-31"))
order(x)
format(sort(x))

}
